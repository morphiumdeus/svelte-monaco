function tryYdoc(ydoc){
    try {
        if(ydoc.getMap('fileMap').)
        return ydoc.getMap('fileMap')
    } catch (e) {
        if (e instanceof TypeError) {
            console.log(e);
        } else {
            console.log(e);
        }
    }
}

export default tryYdoc